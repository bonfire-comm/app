{
  "rules": {
    "channels": {
      "$channel": {
        "messages": {
          "$message": {
            ".write": "auth != null",
            ".indexOn": ["createdAt"]
          }
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}